--- node_modules/playwright-core/lib/server/chromium/crBrowser.js
+++ node_modules/playwright-core/lib/server/chromium/crBrowser.js
@@ -96,6 +96,23 @@
       browser._defaultContext._initialize()
     ]);
     await browser._waitForAllPagesToBeInitialized();
+    // web-plane: release the launch cloak; the standing hidden flag keeps the frame transparent
+    const startMinimized = options.originalLaunchOptions?.args?.includes?.("--start-minimized") && options.headful;
+    if (startMinimized) {
+      try {
+        const path = require("path");
+        const runDir = process.env.WEB_PLANE_RUN_DIR;
+        const runId = process.env.WEB_PLANE_RUN_ID;
+        require("fs").rmSync(path.join(runDir, `.chrome-suppress-${runId}`), { force: true });
+        await new Promise(r => setTimeout(r, 100));
+        const targets = await session.send("Target.getTargets");
+        const pageTarget = targets.targetInfos?.find((t) => t.type === "page");
+        if (pageTarget) {
+          const win = await session.send("Browser.getWindowForTarget", { targetId: pageTarget.targetId });
+          await session.send("Browser.setWindowBounds", { windowId: win.windowId, bounds: { windowState: "normal" } });
+        }
+      } catch {}
+    }
     return browser;
   }
   async doCreateNewContext(options) {
